{% extends 'base.html.twig' %}
{% block title %}Wybierz kurs{% endblock %}
{% block body %}
    {% for category in categories %}
        <div>
            <a class="nav-link" href="{{ path('question_list', {'category': category.getLowerKey()}) }}">{{ category.getValue()|capitalize }}</a>
        </div>
    {% endfor %}
{% endblock %}