{% extends 'base.html.twig' %}

{% block body %}
    <ul class="list-group">
        {% for question in questions %}
        <li class="list-group-item">{{ question.getId() }}</li>
        {% endfor %}
    </ul>
{% endblock %}