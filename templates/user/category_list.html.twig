{% extends 'base.html.twig' %}
{% block body %}
    <div class="card mb-2">
        <h6 class="card-header">Żeglarz jachtowy</h6>
        <div class="card-body">
            <a href="{{ path('user_question_list', {'category': 'zj'}) }}" class=" mb-2 btn btn-primary btn-sm w-100">Przejdź do pytań</a>
        </div>
    </div>
{% endblock %}